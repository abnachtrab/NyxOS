---
# TODO — see docs/ROADMAP.md. Stubbed so site.yml runs end to end.
- name: gpu_intel placeholder
  ansible.builtin.debug:
    msg: "role 'gpu_intel' not implemented yet (profile: {{ nyx_profile }})"
