[terminal]
vt = 1

[default_session]
# Hyprland warns about not being started through a session manager; it runs.
# Switching to uwsm means moving the env vars out of conf.d/env.conf.
command = "tuigreet --time --remember --remember-session --cmd Hyprland"
user = "greeter"
