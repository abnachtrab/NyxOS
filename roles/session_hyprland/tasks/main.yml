---
# TODO — see docs/ROADMAP.md. Stubbed so site.yml runs end to end.
- name: session_hyprland placeholder
  ansible.builtin.debug:
    msg: "role 'session_hyprland' not implemented yet (profile: {{ nyx_profile }})"
